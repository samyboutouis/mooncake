<nav class="navbar navbar-expand-lg navbar-light">
  <% if session[:current_user]["user_type"] == "student"%>
    <a class="navbar-brand" href="<%= root_path %>">
      <div class="container">
        <div class="row">
          <div class="col my-auto">
            <%= image_tag "mooncake_logo.png", class:"logo" %>
          </div> 
          <div class="col my-auto">
            <span id="logo">MOONCAKE<span>
          </div> 
        </div> 
      </div> 
    </a>
  <% else %>
    <a class="navbar-brand" href="<%= faculty_page_path %>">
      <div class="container">
        <div class="row">
          <div class="col my-auto">
            <%= image_tag "mooncake_logo.png", class:"logo" %>
            <span id="logo">MOONCAKE<span>
          </div> 
        </div> 
      </div> 
    </a>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav ml-auto">
      <% if session[:current_user]["user_type"] == "staff" || session[:current_user]["user_type"] == "faculty"%>
        <a class="nav-item nav-link underline" style="text-decoration: none" href="<%= faculty_page_path %>"><span class="nav-tabs">HOME</span><span class="sr-only">(current)</span></a>
      <% else %>
        <a class="nav-item nav-link underline" href="<%= root_path %>"><span class="nav-tabs">HOME</span><span class="sr-only">(current)</span></a>
      <% end %>
      <a class="nav-item nav-link underline" href="<%= student_show_path %>"><span class="nav-tabs">ACCOUNT</span></a>
      <a class="nav-item nav-link underline" href="<%= faq_path %>"><span class="nav-tabs">FAQ</span></a>
      <a class="nav-item nav-link underline" data-method="delete" rel="nofollow" href="<%= logout_path %>"><span class="nav-tabs">LOGOUT</span></a>
    </div> 
  </div> 
  <% if cookies[:moon] %>
    <%= link_to image_tag("sun.png"), sun_path, method: :get, class:"sun"%>
  <% else %>
    <%= link_to image_tag("moon.png"), moon_path, method: :get, class:"moon"%>
  <% end %>
</nav>
