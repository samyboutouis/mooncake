<nav class="navbar navbar-expand-lg navbar-dark" style="background: linear-gradient(180deg, #012169 0%, #0577B1 100%), #0577B1;">
  <a class="navbar-brand" href="#">
    mooncake
    <img class="logo" src="/assets/mooncake_logo.png">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <% if session[:current_user]["user_type"] == "staff"%>
        <a class="nav-item nav-link active" href="<%= faculty_page_path %>">Home<span class="sr-only">(current)</span></a>
      <% else %>  
        <a class="nav-item nav-link active" href="<%= root_path %>">Home<span class="sr-only">(current)</span></a>
      <% end %>
      <a class="nav-item nav-link" href="<%= student_show_path %>">Account</a>
      <a class="nav-item nav-link" href="<%= faq_path %>">FAQ</a>
      <%= link_to 'Log Out', logout_path, class:"nav-item nav-link", method: :delete %>
    </div>
  </div>
</nav>