<nav class="navbar navbar-expand-lg navbar-dark" style="background: rgb(108,27,162);
background: linear-gradient(90deg, rgba(108,27,162,1) 0%, rgba(1,33,105,1) 38%, rgba(190,188,190,1) 92%, rgba(235,225,210,1) 100%);">
  <% if session[:current_user]["user_type"] == "student"%>
  <a class="navbar-brand" href="<%= root_path %>">
    mooncake
    <%= image_tag "mooncake_logo.png", class:"logo" %>
  </a>
  <% else %>
  <a class="navbar-brand" href="<%= faculty_page_path %>">
    mooncake
    <%= image_tag "mooncake_logo.png", class:"logo" %>
  </a>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <% if session[:current_user]["user_type"] == "staff"%>
        <a class="nav-item nav-link " href="<%= faculty_page_path %>">Home<span class="sr-only">(current)</span></a>
      <% else %>
        <a class="nav-item nav-link " href="<%= root_path %>">Home<span class="sr-only">(current)</span></a>
      <% end %>
      <a class="nav-item nav-link" href="<%= student_show_path %>">Account</a>
      <a class="nav-item nav-link" href="<%= faq_path %>">FAQ</a>
      <%= link_to 'Log Out', logout_path, class:"nav-item nav-link", method: :delete %>
    </div>
  </div>
</nav>
